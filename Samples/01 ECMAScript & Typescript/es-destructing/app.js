const contact = {};

function g(arr) {
    const [id, name, ...rest] = arr;

    id;
    name;
}

f(contact);

function f(contact) {
    contact = "Ori";
}

//contact = "X";

contact["name"] = "Ori";

const options = {
    id: 2,
    name: "Ori",
    email: "duiodhiudh",
};

function run({id, name}) {
    // let {id, name} = options;

    id;
    name;

}

run(options);
